---
import type { LinkTarget, VenueLocation } from '../../types/design-system';
import ActionLink from '../ui/ActionLink.astro';

interface Props {
  id: string;
  location: VenueLocation;
  heading?: string;
  directions?: LinkTarget;
  variant?: 'paper' | 'dark';
}

const { id, location, heading = 'Finn oss i Sætre', directions, variant = 'paper' } = Astro.props;
---

<section
  class:list={['location-panel', variant === 'dark' && 'location-panel-dark']}
  aria-labelledby={`${id}-heading`}
>
  <p class="location-panel-kicker">Nordre Sætrevei</p>
  <div class="location-panel-content">
    <h2 id={`${id}-heading`}>{heading}</h2>
    <address>
      <span>{location.name}</span>
      <span>{location.street}</span>
      <span>{location.postalCode} {location.city}</span>
    </address>
  </div>
  <span class="location-panel-coordinate" aria-hidden="true">Mike’s Pub / 3475 Sætre</span>
  {directions && <ActionLink {...directions} />}
</section>
